@model IEnumerable<DAL.App.DTO.Compatibility>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

@if (User.IsInRole("Admin"))
{
    <p>
        <a asp-action="Create">Create New</a>
    </p>
}
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CompatibilityType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GameOnPlatform)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Emulator)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CompatibilityType!.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GameOnPlatform!.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Emulator!.Name)
            </td>
            <td>
                @if (User.IsInRole("Admin"))
                {
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                    <text> | </text>
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a>
                    <text> | </text>
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                }
                else
                {
                    <a asp-action="Details" asp-route-id="@item.Id">Details</a>
                }
            </td>
        </tr>
}
    </tbody>
</table>
